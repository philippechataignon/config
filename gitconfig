[user]
    name = philippe
    email = philippe@chataignon.com

[alias]
    st = status
    ci = commit
    co = checkout
    br = branch -v
    d =  diff
    lg = log --pretty=format:'%Cred%h% %Cblue%d% Creset%s %Cblue%cr %Cgreen(%an)%Creset'
    l = log --pretty=format:'%Cred%h% %Cblue%d% Creset%s %Cblue%cr %Cgreen(%an)%Creset' --graph --all
    f = fetch --prune -v
    r = remote -v

[merge]
    tool = meld
    defaultToUpstream = True

[core]
    editor = vim
    pager = more

[color]
    ui = auto

[push]
    default = upstream

[credential "https://github.com"]
	username = philippechataignon

[credential "https://bitbucket.org"]
	username = philippechataignon

;[http]
;    proxy = http://proxy2.justice.gouv.fr:8080
